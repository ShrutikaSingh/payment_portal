extends layout

block content
  h1 #{title}
  form(method="post",action='/workshop/payment')  /*we will catch this in app js*/
    #form-group
      label Title:
      input.form-control(name='title',type='text')
    #form-group
      label Author:
      input.form-control(name='author',type='text')
    #form-group
      label Body:
      input.form-control(name='body')
    input.btn.btn-primary(type='submit',value='submit')
  
